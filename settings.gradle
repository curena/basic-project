//necessary in order to get experimental spring native deps
pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/snapshot' }
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = 'basic'
